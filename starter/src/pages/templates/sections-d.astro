---
/** Template preview route (§17): final CTA + footer (footer mounts via layout). noindex. */
import BaseLayout from '../../layouts/BaseLayout.astro';
import FinalCTA from '../../components/FinalCTA.astro';
---

<BaseLayout title="Template preview — final CTA + footer" description="Internal template preview." noindex>
  <FinalCTA
    heading="Don't Wait — Damage Gets Worse Every Hour"
    support="The sooner mitigation starts, the more of your property we can save. Call now and talk to a real person."
  />
</BaseLayout>
