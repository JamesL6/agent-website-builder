# Gate 4 visual packet — convention

`artifacts/design/gate4/` holds the §24 screenshot set the human approves the design FROM:
`mobile-initial.png` (390×844), `mobile-scrolled.png` (390×844), `desktop.png` (1440×1100),
`tablet.png` (1024×900) — of the THEMED starter previews (`/templates/homepage/` with the approved
Messaging Pack copy, `/templates/inner-page/`, `/templates/contact-page/`) after `theme.css`, the
logo, and `site.ts` NAP are applied. `validate:artifacts` refuses an Approved Design Recipe without
these four files. The PNGs here are 8×8 stand-ins so the fixture validates; a real packet is real
screenshots.
